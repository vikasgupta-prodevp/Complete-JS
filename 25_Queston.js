//First method to decleraing function 
function addone(num){
    return num+1
}
addone(10);

// second method to declaring function using variable
const addtwo=function(num){
    return num+2
}
addtwo(2)//it will thorugh an error when we declare addtwo() before variable const